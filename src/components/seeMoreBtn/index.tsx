import styles from '@/components/seeMoreBtn/seeMoreBtn.module.css';

export default function SeeMoreBtn() {
    return (
        <div>
            <button className={styles.btn}>See More &gt;</button>
        </div>
    );
}
